# ============================================================
# SyncSpace — Root Environment Variables
# ============================================================
# This file is shared by both server and web apps.
# Copy to .env and fill in real values before running.
# NEVER commit .env — only .env.example is safe for Git.
# ============================================================

# ===== SERVER =====
NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:3000

# ===== DATABASE =====
MONGODB_URI=mongodb://localhost:27017/syncspace

# ===== JWT (generate 256-bit+ random strings) =====
JWT_ACCESS_SECRET=replace-with-secure-random-access-secret
JWT_REFRESH_SECRET=replace-with-secure-random-refresh-secret
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# ===== COOKIES =====
COOKIE_DOMAIN=localhost
COOKIE_SECURE=false

# ===== EMAIL (Resend — https://resend.com) =====
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=SyncSpace <noreply@yourdomain.com>

# ===== CLOUDINARY (optional — file uploads) =====
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ===== CLIENT (Next.js public vars) =====
NEXT_PUBLIC_API_URL=http://localhost:5000/api
NEXT_PUBLIC_SOCKET_URL=http://localhost:5000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===== WebRTC TURN Server (optional — production only) =====
TURN_SERVER_URL=turn:your-turn-server.example.com:3478
TURN_USERNAME=your_turn_username
TURN_CREDENTIAL=your_turn_credential
